# Note for external contributors: this points at an internal Microsoft feed. If the
# feed is not public you will need to override the registry locally, e.g.
#   npm install --registry=https://registry.npmjs.org/
registry=https://skype.pkgs.visualstudio.com/SCC/_packaging/SCC_PublicPackages/npm/registry/
always-auth=true
